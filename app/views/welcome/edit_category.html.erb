<h1>修改種類名稱</h1>

<%= form_for(@category, :url => edit_category_path) do |f| %>
	<div class="field">
		<%= f.text_field :name %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>

<% end %>

<%= link_to "back", category_path %>